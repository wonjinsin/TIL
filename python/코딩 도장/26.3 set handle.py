a = {1, 2, 3, 4}
a.add(5)  # {1, 2, 3, 4, 5}

a.remove(3)  # {1, 4, 5}, 요소 없으면 에러
a.discard(3)  # {1, 4, 5}, 요소 없으면 무시

a = {1, 2, 3, 4}
a.pop()  # 1

a.clear()  # 요소 전부 삭제
set()

a = {1, 2, 3, 4}
len(a)
